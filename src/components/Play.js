import React from 'react';
import styled from 'styled-components';

const PlayStyle = styled.div `
  *{
    background : #fff;
  }
`;
const Play= () => {
  return(
    <div>a</div>
  )
}

export default Play;