import React from 'react';
import styled from 'styled-components';

const JoinStyle = styled.div `
  *{
    background : #fff;
  }
`;
const Join= () => {
  return(
    <div>a</div>
  )
}

export default Join;