import React from 'react';
import styled from 'styled-components';

const MakeStyle = styled.div `
  *{
    background : #fff;
  }
`;
const Make= () => {
  return(
    <div>a</div>
  )
}

export default Make;