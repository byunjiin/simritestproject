import React from 'react';
import styled from 'styled-components';

const NavStyle = styled.div `
  *{
    background : #fff;
  }
`;
const Nav= () => {
  return(
    <div>a</div>
  )
}

export default Nav;